# ==============================================
# Biến Môi Trường cho SimpleCommerce MDM Backend
# ==============================================

# -- Cấu Hình Chung cho Project
# Được Docker Compose sử dụng làm tiền tố cho tên container để tránh xung đột.
COMPOSE_PROJECT_NAME=simplecommerce

# -- Cấu Hình Ứng Dụng Backend
# Cổng cho API backend (Spring Boot)
SERVER_PORT=8080
# Profile Spring đang hoạt động (dev, prod, test)
SPRING_PROFILES_ACTIVE=dev
# Tùy chọn cho máy ảo Java (JVM)
JAVA_OPTS=-Xmx512m -Xms256m

# -- Cấu Hình Database (PostgreSQL)
# Tên người dùng (username) cho PostgreSQL
POSTGRES_USER=your_db_username
# Mật khẩu cho người dùng PostgreSQL. HÃY THAY ĐỔI trong file .env của bạn.
POSTGRES_PASSWORD=your_secure_db_password
# Tên của database
POSTGRES_DB=simplecommerce_mdm
# Cổng bên ngoài để truy cập PostgreSQL
POSTGRES_PORT=5432

# -- Cấu Hình Redis
# Cổng bên ngoài để truy cập Redis
REDIS_PORT=6379

# -- Cấu Hình pgAdmin
# Email mặc định để đăng nhập pgAdmin.
PGADMIN_DEFAULT_EMAIL=admin@simplecommerce.com
# Mật khẩu mặc định cho pgAdmin. HÃY THAY ĐỔI trong file .env của bạn.
PGADMIN_DEFAULT_PASSWORD=your_pgadmin_password
# Cổng bên ngoài để truy cập pgAdmin
PGADMIN_PORT=5050

# -- Cấu Hình Frontend
# URL của ứng dụng frontend, dùng để cấu hình CORS.
FRONTEND_URL=http://localhost:5173

# ==============================================
# Cấu hình Bảo mật / JWT  
# ==============================================
# JWT Keys - Generate new secure keys for your environment
JWT_ACCESS_KEY=your_jwt_access_key_minimum_64_characters_for_security_purposes
JWT_REFRESH_KEY=your_jwt_refresh_key_minimum_64_characters_for_security_purposes

# ==============================================
# Cấu hình OAuth2 (Google)
# ==============================================
# Google OAuth credentials from Google Cloud Console
GOOGLE_CLIENT_ID=your_google_oauth_client_id
GOOGLE_CLIENT_SECRET=your_google_oauth_client_secret

# ==============================================
# Các cài đặt cho môi trường Production (nếu cần)
# ==============================================
# PROD_DB_URL=jdbc:postgresql://your_prod_db_host:5432/simplecommerce_mdm_prod
# PROD_DB_USER=simplecommerce_user
# PROD_DB_PASSWORD=your_secure_password_here

# ==============================================
# Cấu hình Dịch vụ bên ngoài (External Services)
# ==============================================
# API Key cho dịch vụ email Brevo (trước đây là Sendinblue)
BREVO_API_KEY=your_brevo_api_key_here

# Cloudinary Credentials for file uploads
CLOUDINARY_CLOUD_NAME=your_cloudinary_cloud_name
CLOUDINARY_API_KEY=your_cloudinary_api_key
CLOUDINARY_API_SECRET=your_cloudinary_api_secret

# ==============================================
# Hướng dẫn:
# 1. Sao chép file này thành .env
# 2. Xem lại và thay đổi các giá trị mặc định (đặc biệt là mật khẩu).
# 3. Không bao giờ commit file .env lên Git.
# ============================================== 